import React from 'react';
import { Container, Typography, Paper, Box } from '@mui/material';

// Create all customer page components as placeholders
export const CustomerDashboard = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Customer Dashboard</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>Customer dashboard - To be implemented</Typography>
    </Paper>
  </Container>
);

export const HotelSearch = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Hotel Search</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>Hotel search functionality - To be implemented</Typography>
    </Paper>
  </Container>
);

export const HotelDetails = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Hotel Details</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>Hotel details page - To be implemented</Typography>
    </Paper>
  </Container>
);

export const BookingConfirmation = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Booking Confirmation</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>Booking confirmation page - To be implemented</Typography>
    </Paper>
  </Container>
);

export const MyBookings = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>My Bookings</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>My bookings page - To be implemented</Typography>
    </Paper>
  </Container>
);

export const CustomerProfile = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Customer Profile</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>Customer profile page - To be implemented</Typography>
    </Paper>
  </Container>
);
