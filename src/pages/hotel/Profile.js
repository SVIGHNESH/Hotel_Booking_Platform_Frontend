import React from 'react';
import { Container, Typography, Paper } from '@mui/material';

const Profile = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4" gutterBottom>Hotel Profile</Typography>
    <Paper elevation={3} sx={{ p: 4 }}>
      <Typography>Hotel profile management page - To be implemented</Typography>
    </Paper>
  </Container>
);

export default Profile;
